import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Goal = () => {
  return (
    <View>
      <Text>Goal</Text>
    </View>
  );
};

export default Goal;

const styles = StyleSheet.create({});
